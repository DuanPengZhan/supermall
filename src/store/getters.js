export default {
    cartLength(state) {
        return state.cartList.length;
    },
    cartList(state){
        return state.cartList
    },
    // tabClickIndex(state){
    //     return state.tabClickIndex
    // }
    
}